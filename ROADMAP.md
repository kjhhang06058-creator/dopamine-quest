# Dopamine Quest 로드맵 / 프로젝트 메모리

이 문서는 "지금 무엇이 실제로 되어 있고, 무엇이 다음 목표인지"를 정확히 유지하기 위한 기록입니다. 외부에서 받은 전략 문서와 실제 코드베이스 상태가 어긋나는 걸 막기 위해 만들었습니다 — 업데이트할 때마다 실제 커밋 기준으로 검증하고 고치세요.

## 현재 상태 (실제로 구현되어 GitHub에 푸시됨)

| 영역 | 상태 |
| --- | --- |
| Frontend | Next.js 14 (App Router) + TypeScript + Tailwind, 3탭 구조(퀘스트/보스 레이드/상점) |
| 상태 관리 | Zustand + `persist` 미들웨어로 `localStorage` 자동 저장 |
| 연출 | Framer Motion — 완료 시 중앙 존 반응, 플로팅 보상/크리티컬 텍스트, 실패 시 화면 붉은 플래시 |
| 보상 시스템 | 퀘스트 완료 시 변동성 골드/EXP(크리티컬 12%). **웨이브/몬스터 HP는 제거됨** — 대신 누적 `careerExp` 100점마다 구간 보상(18G + 구간당 2G), 승급 시 150G |
| 커리어 트랙 | `types/career.ts`에 3개 트랙 × 5티어 정의. 🏛️공무원(수험생→필기합격→시보→주무관→5급 사무관), ⚖️전문직(수험생→1차합격→수습→어소→대표/파트너), 🎮판타지(모험가→견습기사→기사→기사단장→영웅). `CareerHud`에 트랙 전환 드롭다운·등급 배지·진행 미터·멘탈(HP) 바, 티어 통과 시 `PromotionModal`(레트로 도장 + 컨페티) 자동 발동. 완료 플로팅 문구가 티어별 `actionVerb`로 바뀜("문서 결재 완료", "사건 수임 완료!", "크리티컬 히트!"). 트랙 전환해도 `careerExp`는 공유되어 진행도가 사라지지 않음 |
| 중앙 존 | `CareerZone`(구 `MonsterZone`) — 트랙·티어별 대상이 표시됨(공무원: 📚수험 진도→📝면접 준비→🗂️인계 업무함→📋결재 서류함→🏛️정책 과제 / 전문직: 📚→📝→📁수습 사건→⚖️수임 사건→🏢사무소 / 판타지: 기존 몬스터). 게이지는 "HP 깎기"가 아니라 "승급까지 N 남음"을 채우는 방식 |
| 집중 모드 | 보스 레이드 = 집중 타이머. 5/10/15/30분·1시간·2시간 + 직접 설정(1~180분), 시간이 길수록 강한 대상. 대상/문구도 트랙별(공무원: 📮민원→📄문서 검토→📝보고서→🗂️결재 라인→🏛️정책 기획→📊국정감사 대비→🔥예산안 마감). 탭 이탈/화면 꺼짐 시 즉시 패배, 완주 시 대량 보상 + 확정 에픽 이상 전리품. 진행 중 Wake Lock으로 화면 자동 잠금 방지(수동 전원 off는 웹 API로 구분 불가) |
| 상점 | 골드 가챠(4등급 확률 테이블), 부활의 물약(40G), **프리미엄 테마 실제 적용**(사이버펑크/다크판타지/게임보이, 250~300G) — 구매 즉시 배경·헤더·중앙 존·하단 네비 색상이 CSS 변수로 교체됨. 인앱결제는 여전히 미연동(게임 내 골드로만 구매) |
| 목표 모드 | 사전 할당 도파민 보상 스케줄러 — 퀘스트에 목표 횟수/사전 보상(실물)/마일스톤(기본 80%)을 설정하면, 마일스톤 도달 시 축하 팝업, 목표 완주 시 기존 몬스터 공격 보상. 팝업에서 "보상 섭취" 시 +30 HP 즉시 회복 + 다음 퀘스트 완료에 1.5배 EXP 버프(HUD에 EXP UP 배지 표시) |
| 일일 예약 보상 | 목표 모드와 별개로, 퀘스트 탭 최상단 `PrePlannedRewardCard`에서 "오늘 하루 전체" 보상 1개를 예약(제목 + 70/80/90% 마일스톤). 진행률은 **오늘 기준(미완료 + 오늘 완료한 것)** 으로만 계산되고(`Task.completedAt`), 예약은 `DailyReward.date`로 당일에만 유효 — 어제 예약분은 만료되어 다시 예약 폼이 뜸. 마일스톤을 넘으면 `MilestoneBreakModal`(강제 휴식 팝업)이 자동으로 뜨고 "보상 섭취"로 +30 HP + EXP 버프. 목표 모드와 독립적으로 공존 |
| 태스크 편집 | 제목을 눌러 인라인 수정(Enter 확정 / Esc 취소 / 한글 IME 조합 중 Enter 무시), 완료 목록 "전체 지우기" 버튼 |
| AI 퀘스트 분해기 | 별도 버튼 없음 — 퀘스트 탭에서 "추가"를 누르면 `/api/decompose`가 제목을 자동으로 검사해 복잡해 보이면(키워드 규칙 기반: 청소/공부/운동/빨래/설거지/이메일/준비·이사·프로젝트 + 글자 수 임계값) `TaskSplitModal`이 "이 작업, 나눠볼까요?" 팝업을 띄움. 짧고 단순한 제목은 팝업 없이 바로 추가됨. 수락 시 `addBatchTasks`로 3개 마이크로 퀘스트 일괄 등록, 거절해도 원래 입력한 제목으로 정상 추가(입력이 버려지지 않음). 목표 모드는 이미 수동 다단계 설정이라 이 검사를 건너뜀. 진짜 LLM 호출 아님(무료 규칙 기반) |
| 안티 길트 & 스트릭 실드 | 로컬 날짜(`YYYY-MM-DD`) 기준 연속 완료일을 `streakDays`로 추적(자정 리셋/타임존 정교화는 없음 — 태스크 완료 시점마다 "오늘" 갱신만 하는 가벼운 버전). 하루를 통째로 건너뛰면 보유 중인 "부활의 물약"(`streakShields`, 상점에서 40골드 구매)을 자동 소모해 스트릭 보호, 없으면 죄책감 유발 문구 없이 1일부터 재시작. `실패 처리` 버튼을 연속 2회 클릭하면 복귀 지원으로 다음 완료 EXP 1.5배 버프(몬스터 체력 감소 방식이었으나 웨이브 제거로 대체), 완료 성공 시 연속 실패 카운트 리셋 |
| 백엔드 | Supabase Auth(이메일 매직 링크 + Google OAuth) + PostgreSQL + RLS, 로그인 시 로컬↔클라우드 세이브를 `updatedAt` 기준으로 병합, 이후 변경사항 디바운스 업로드 |
| 배포/협업 | 독립 GitHub 저장소(`kjhhang06058-creator/dopamine-quest`), 로컬 빌드/개발 서버 검증 완료 |

**아직 안 된 것**:
- Supabase 프로젝트는 생성됨(URL/anon key 확보), 스키마도 사용자가 SQL Editor에서 실행함. 다만 이 세션의 샌드박스 환경이 `supabase.co`로 나가는 아웃바운드 네트워크를 정책상 차단하고 있어, 로그인/동기화 실동작은 사용자가 자신의 로컬 컴퓨터에서 직접 검증해야 함(아직 확인 안 됨).
- Google OAuth는 코드/버튼까지 준비됐지만, Google Cloud Console에서 OAuth 클라이언트를 만들고 Supabase Provider에 등록하는 건 사용자 몫 — 아직 안 함.

## 다음 후보 (우선순위 미확정 — 사용자 확인 후 착수)

1. 로컬 환경에서 매직 링크/Google 로그인 + 클라우드 동기화 실동작 검증 (README의 "클라우드 동기화 설정" 절차대로)
2. 프리미엄 테마/스킨 실제 인앱결제 연동
3. (선택) 스트릭을 "정식 매일 리셋 시스템"으로 승격 — 지금은 자정 리셋/타임존 없이 완료 시점마다 날짜만 비교하는 가벼운 버전. 진짜 습관 추적 앱처럼 만들려면 하루 경계를 명확히 정의하는 재설계가 필요.

## 의도적으로 보류한 것 (지금 만들지 않기로 한 것)

핵심 루프(퀘스트 → 보스 레이드 → 상점)가 실사용자에게 리텐션을 만드는지 검증되지 않은 상태에서, 아래 항목은 "기능 추가"가 아니라 사실상 별도 제품 규모라 지금 설계에 넣지 않습니다. 핵심 루프가 검증된 뒤 재검토합니다.

- 소셜 파티 레이드 / 바디 더블링 (친구 그룹, 공동 보스, 상호 책임감 페널티) — 실시간 다인원 상태 동기화가 필요한 별도 백엔드 설계
- B2B ADHD 코칭/상담센터용 리포트 대시보드 — 완전히 다른 사용자층(코치)을 위한 별도 제품

## 참고: 이 문서를 다시 쓰게 만든 반복 패턴

외부(다른 AI 세션/Claude Pro 등)에서 받은 전략 문서가 이미 완료된 작업을 "다음 단계"로 다시 제안하는 일이 두 번 있었습니다(Supabase 연동을 처음부터 다시 만들자는 제안 등). 새 전략 문서를 받으면 반드시 이 표와 대조부터 하고, 이미 된 것을 다시 만들자는 제안이면 사용자에게 먼저 확인하세요.

## 작업 이력 요약

- MVP 초기 구현 (퀘스트/보스 레이드/상점, Zustand, Framer Motion) — 커밋 `99779b3`
- Supabase 매직 링크 로그인 + 클라우드 동기화 — 커밋 `d960a86`
- Google OAuth 로그인 추가 — 커밋 `412d854`
- 사전 할당 도파민 보상 스케줄러 (목표 모드, DopamineBar, RewardBurstModal) — 커밋 `f73409b`
- 보상 섭취 시 HP 회복 + EXP 버프 (claimReward, expBuffActive) — 기존 마일스톤 팝업 확장
- 일일 예약 보상 카드 + 강제 휴식 팝업 (PrePlannedRewardCard, MilestoneBreakModal, dailyReward) — 목표 모드와 별개의 전역 보상 시스템 신설
- AI 퀘스트 3단계 분해기 (`/api/decompose`, `TaskSplitModal`, `addBatchTasks`) — 규칙 기반 마이크로 퀘스트 생성. 최초엔 수동 버튼이었다가, 퀘스트 추가 시 자동 복잡도 판단 + 확인 팝업 방식으로 재설계. 이후 절차형("준비→5분→마무리")에서 **내용/영역형**으로 전환하고 규칙을 80개 이상으로 확장(법학·공무원 직렬별·공기업 NCS·수능/중고등 과목 및 줄임말·전문자격사)
- 프리미엄 테마 실제 적용 (`lib/themes.ts`, CSS 변수 `--theme-*`, `selectTheme`)
- 커리어 트랙 시스템 (`types/career.ts`, `CareerHud`, `PromotionModal`) — 3트랙 × 5티어, 승급 모달
- 웨이브/몬스터 제거 → 티어 기반 진행으로 전환 (`CareerZone`, `careerExp`/`careerSegment`). 웨이브 클리어 보너스를 대체할 구간 보상 + 승급 보너스로 골드 경제 재설계
- 자체 감사 수정: 일일 진행률이 과거 완료분까지 세던 버그, 일일 보상 미만료 버그, 승급 컨페티 고정 궤적 버그, 제목 수정 불가, 완료 목록 무한 누적, HP 중복 표시
- 안티 길트 시스템 & 스트릭 실드 (`streakDays`, `lastActiveDate`, `streakShields`, `consecutiveFails`, `buyStreakShield`) — 연속 기록 보호 + 연속 실패 시 몬스터 체력 다운스케일링
